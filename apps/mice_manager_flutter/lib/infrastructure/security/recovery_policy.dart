class RecoveryPolicy {
  const RecoveryPolicy();

  String ownerRecoveryPlaceholder() {
    // TODO(recovery-key-support): Replace placeholder with secure recovery phrase / recovery key flow.
    return 'TODO-RECOVERY-KEY';
  }
}
